/**
 * 法1：从左下角开始
 * 法2：坐标轴法（坐标移动）
 * 法3：模拟bs，从矩阵中心开始判断
 * @param {number[][]} matrix
 * @param {number} target
 * @return {boolean}
 */
var searchMatrix = function(matrix, target) {
    if (matrix.length < 1 || matrix[0].length < 1) return false;
    // 保证至少为 1 x 1 矩阵
    
};

