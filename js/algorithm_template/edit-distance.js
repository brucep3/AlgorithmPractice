/**
 * 读题：插入，删除，替换
 * @param {string} word1
 * @param {string} word2
 * @return {number}
 */
var minDistance = function(word1, word2) {
    // | word1.length - word2.length |
    // -> 最长公共子序列
    // dp[i][j] 表示
};